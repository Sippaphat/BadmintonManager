version: '3.8'

services:
  # ----------------------------
  # 1. Backend Service (Node API)
  # ----------------------------
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: badminton-api
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    volumes:
      # CRITICAL: This saves uploaded photos on your server disk
      - ./server/uploads:/app/server/uploads
    restart: always

  # ----------------------------
  # 2. Frontend Service (React)
  # ----------------------------
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: https://badminton-api.xenocer.com
    container_name: badminton-web
    ports:
      - "8000:8000"  # Expose on standard web port
    restart: always
    depends_on:
      - backend